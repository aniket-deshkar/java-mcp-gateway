# Contributing

Create a focused branch and keep transport adapters separate from policy logic. Add positive, negative, boundary, and failure-semantic tests for behavior changes.

Run `./mvnw verify` before opening a pull request. Do not commit credentials, local databases, build output, or captured tool payloads.
